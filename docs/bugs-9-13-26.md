Accountant User Role:

1. on /student/id page, The attendance grid has an incorrect date and day mapping or the month dates are not displaying correctly however the data being fetched is correct. The issue is with the display of the dates in the attendance grid. The dates should be aligned correctly with the corresponding days of the week and should reflect the correct month.

2. Fees for kirtan student is not auto generating per month. The system should generate fees for all classes per month as it is done for Gurmukhi already.

Admin User Role:

1. The All Students page has a css issue. The student modal, as i view or try to change enrollment doesn't scroll on mobile or might be not scrolling at all.

2. I need a a search bar on the attendace page where i can atleast filter student by name or father name

3. In the Classes page where i can manage my classes, there are 2 buttons. 1 is add class and the other is new class. i want you to remove the redundant or old one, i added the one recently and make sure the only button has no bugs with ui and ux.
